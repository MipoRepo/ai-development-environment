# OpenRouter API key (https://openrouter.ai/keys)
OPENROUTER_API_KEY=sk-or-v1-placeholder

# Default model used across agents (override per-task if needed)
DEFAULT_MODEL=openai/gpt-4o-mini

# App-level config
APP_ENV=development
LOG_LEVEL=INFO
